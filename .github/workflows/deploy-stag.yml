name: deploy to stag

on:
  workflow_run:
    workflows: [build]
    branches: [main]
    types:
      - completed
      
  workflow_dispatch:
    inputs:
      artifact_name:
        required: true
        type: string
        description: artifact build name to deploy (retrieve from build workflow)

jobs:
  deploy-stag-auto:
    uses: iccowan/reusable-workflows-test/.github/workflows/deploy.yml@main
    with:
      dep_env: 'stag'
      artifact_name: ${{ github.event.workflow_run.env.ARTIFACT_NAME }}
      
    if: ${{ github.event_name == 'workflow_run' }}
      
  deploy-stag-manual:
    uses: iccowan/reusable-workflows-test/.github/workflows/deploy.yml@main
    with:
      dep_env: 'stag'
      artifact_name: ${{ inputs.artifact_name }}
    
    if: ${{ github.event_name == 'workflow_dispatch' }}
